[project]
name = "mono-arcive"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "textual>=8.2.8",
    "typer>=0.26.8",
]

[project.scripts]
mono-archive = "mono_archive.main:app"
mono-pub = "mono_archive.main:app"
